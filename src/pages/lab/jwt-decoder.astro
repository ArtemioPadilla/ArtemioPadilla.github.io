---
import BaseLayout from "../../components/layout/BaseLayout.astro";
import Navigation from "../../components/layout/Navigation.astro";
import Footer from "../../components/layout/Footer.astro";
import JwtDecoder from "../../components/lab/jwt-decoder/JwtDecoder.tsx";
---

<BaseLayout
  title="JWT Decoder — Lab — Artemio Padilla"
  description="Decode and inspect JSON Web Tokens. See header, payload, claims, and expiry status."
>
  <Navigation currentPath="/lab" />

  <main class="px-6 pt-32 pb-24 md:pt-40 md:pb-32">
    <div class="mx-auto max-w-5xl">
      <!-- Breadcrumb -->
      <div class="mb-6 flex items-center gap-2 text-xs text-[var(--color-text-muted)]">
        <a href="/lab" class="no-underline transition-colors hover:text-[var(--color-heading)]">Lab</a>
        <span>/</span>
        <span class="text-[var(--color-heading)]">JWT Decoder</span>
      </div>

      <!-- Header -->
      <div class="mb-8">
        <div class="flex items-center gap-3">
          <span class="text-3xl">🔑</span>
          <h1 class="font-heading text-3xl font-bold uppercase text-[var(--color-heading)] sm:text-4xl">
            JWT Decoder
          </h1>
        </div>
        <p class="mt-3 max-w-xl text-sm leading-relaxed text-[var(--color-text-muted)] sm:text-base">
          Decode and inspect JSON Web Tokens. See claims, timestamps, and expiry status with live countdown.
        </p>
      </div>

      <!-- Tool -->
      <JwtDecoder client:visible />

      <!-- Info -->
      <div class="mt-6 text-xs text-[var(--color-text-muted)]">
        <p>Your token never leaves your browser. Signature verification is not performed — only decoding.</p>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>
